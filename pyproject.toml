[project]
name = "adk-agent-geap-ai-gw"
version = "0.1.0"
description = "ADK agent probing Agent Gateway ingress/egress inspection over Agent Retrieval"
requires-python = ">=3.10"

[tool.ruff]
line-length = 80
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
